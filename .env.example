# =====================================================
# FRONTEND ENVIRONMENT (VITE_ prefix)
# =====================================================

# AI Service Configuration
# Set to 'real' to use actual LLM API, 'mock' for mock responses (testing)
VITE_AI_MODE=mock

# Supabase Client Configuration
# Get your values from: https://app.supabase.com/projects
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Rewriter API Toggle
# Set to 'true' to enable real Rewriter API (requires ANTHROPIC_API_KEY on backend)
VITE_REWRITER_ENABLED=false

# =====================================================
# BACKEND ENVIRONMENT (Vercel Serverless Functions)
# =====================================================

# Google Gemini API Key (for Rewriter)
# Get yours: https://makersuite.google.com/app/apikey
# REQUIRED: Set this to use real Rewriter API
# Only needed on backend (api/rewrite.ts), NOT exposed to frontend
GOOGLE_API_KEY=your_api_key_here

# Supabase Backend Config (for auth verification in api/rewrite.ts)
# Same values as frontend, used by Vercel functions
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# =====================================================
# DEVELOPMENT NOTES
# =====================================================

# For Local Development:
# 1. Copy this file to .env.local
# 2. Fill in actual values
# 3. Install: npm install -D @vercel/node
# 4. Dev server must have api/ directory available (Vite proxy or separate)
#
# For Production (Vercel):
# 1. Set GOOGLE_API_KEY in Vercel project settings
# 2. Frontend calls /api/rewrite (automatic)
# 3. Admin users only have access
#
# Gemini API Setup:
# 1. Go to: https://makersuite.google.com/app/apikey
# 2. Create new API key
# 3. Copy to GOOGLE_API_KEY (both local and Vercel)
